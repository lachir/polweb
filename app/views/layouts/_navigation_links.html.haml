%li{:class => (if params[:controller] == 'projets' then ' bg-warning' else '' end)}
  = link_to projets_path do
    Projets
%li.dropdown{:class => (if params[:controller] == 'recherche' then 'dropdown bg-warning' else 'dropdown' end)}
  %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
    Donnés de base
    %span.caret
  %ul.dropdown-menu{:role => "menu"}
    %li
      = link_to recherche_g_path do
        Combustible gazeux
    %li
      = link_to recherche_l_path do
        Combustible liquide
    %li
      = link_to recherche_s_path do
        Combustible solide
    %li
      = link_to recherche_a_path do
        Combustible autre
    %li
      = link_to recherche_m_path do
        Matières
    %li
      = link_to recherche_u_path do
        Utilités
    %li
      = link_to recherche_c_path do
        Conditions
%li{:class => (if params[:controller] == 'clients' then ' bg-warning' else '' end)}
  = link_to clients_path do
    Clients
%li{:class => (if params[:controller] == 'offres' then ' bg-warning' else '' end)}
  = link_to offres_path do
    Offres
%li{:class => (if params[:controller] == 'items' then ' bg-warning' else '' end)}
  = link_to items_path do
    Items
%li{:class => (if params[:controller] == 'prix_poids' then ' bg-warning' else '' end)}
  = link_to prix_poids_path do
    Items avec offres
%li{:class => (if params[:controller] == 'fournisseurs' then ' bg-warning' else '' end)}
  = link_to fournisseurs_path do
    Fournisseurs
%li{:class => (if params[:controller] == 'codification' then ' bg-warning' else '' end)}
  = link_to 'Codification', controller: 'codification', action: 'index'
- if session[:ecriture]
  %li
    = link_to '/signout' do
      %span.glyphicon.glyphicon-off
- else
  %li
    = link_to new_session_path do
      %span.glyphicon.glyphicon-pencil

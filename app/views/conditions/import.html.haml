%h3 Importer les conditions pour le projet #{@projet_qui_recoit.code_complet + @projet_qui_recoit.nom_code}
= form_tag(action="importer_conditions") do
	/
		Genere une liste de projets qui ont des conditions
	%h4 Depuis le projet
	= select_tag "IdInput", options_for_select(@projet_eligible.order(code_complet: :asc).collect{ |u| [u.code_complet + ' '+ u.nom_code, u.id] })
	= submit_tag 